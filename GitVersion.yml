mode: ContinuousDelivery
major-version-bump-message: '\+semver:\s?(breaking|major)|(BREAKING CHANGE)'
minor-version-bump-message: '\+semver:\s?(feature|minor)|^(feat|add|new):'
patch-version-bump-message: '\+semver:\s?(fix|patch)|^(fix|bug):'
no-bump-message: '\+semver:\s?(none|skip)|^(docs|ci|test|chore|refactor):'
commit-message-incrementing: Enabled

branches:
  master:
    tag: ''
    increment: Patch
    regex: ^master$
    is-release-branch: true
    
  stable:
    tag: rc
    increment: Patch
    regex: ^stable$
    is-release-branch: true
    source-branches: ['master']
    
  dev:
    tag: beta
    increment: Minor
    regex: ^dev$|^develop$
    source-branches: ['master']
    
  feature:
    tag: alpha
    increment: Minor
    regex: ^feature[/-].+
    source-branches: ['dev', 'master']
    
  hotfix:
    tag: hotfix
    increment: Patch
    regex: ^hotfix[/-].+
    source-branches: ['master']
    
  support:
    tag: support
    increment: None
    regex: ^support[/-].+
    source-branches: ['master']
    
  release:
    tag: rc
    increment: None
    regex: ^release[/-].+
    source-branches: ['dev']
    is-release-branch: true
 